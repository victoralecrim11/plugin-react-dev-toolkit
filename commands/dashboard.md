---
description: Inicia o Project Hub local para projetos, componentes, reviews e checklist.
---
# /dashboard

Use a skill `dashboard-projetos`. Copie os arquivos de `references/` para uma pasta local de trabalho (ou execute-os no local atual) e rode `iniciar-dashboard.bat` no Windows ou `./iniciar-dashboard.command` no macOS/Linux. O painel abre em `http://127.0.0.1:8766` e persiste em `projetos-data.json`.
